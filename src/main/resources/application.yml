spring:
  application:
    name: service-registry
server:
  port: 8765

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
  renewalPercentThreshold: 0.85